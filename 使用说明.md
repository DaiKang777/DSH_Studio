# Harness 社区个人版：老头骑鲸鱼图标版

双击本目录中的 Setup.exe，按安装向导完成安装。关闭正在运行的 Harness 后再安装；建议先备份 %APPDATA%\HarnessCommunityTest。无需先卸载旧版。

本包版本仍为 0.1.6-alpha.2，构建标识 mirrors-icon-2，需要手动安装；应用内更新不会提示同版本升级。保留原应用标识、注册安装位置、用户数据目录及可选镜像更新功能。安装包默认未填写镜像地址，首次使用需按下方说明添加 GitHub 镜像。

桌面快捷方式、程序 EXE、安装器、卸载器及程序使用的 Windows 图标换成已确认的老头骑鲸鱼。软件聊天页面及安装向导内的装饰插图仍沿用原图。图标源文件一并提供。

本包未签名，Windows 可能出现安全提示；请确认来源，不要关闭安全软件。若安装后旧快捷方式仍显示旧图标，先重新打开程序或重启电脑，Windows 可能缓存旧图标。

本次没有在你的电脑执行安装。干净系统上的安装、升级、卸载及桌面与任务栏实际显示仍需安装后验证。

## 在软件里设置 GitHub 更新镜像

设置位置在 Harness 社区个人版软件内，不在 GitHub 网页。

1. 打开软件，在窗口顶部菜单中找到“更新与镜像…”。如果菜单栏没有显示，可先按一下 Alt 键。
2. 在“添加镜像”区域，镜像名称填写 `GitHub`，镜像目录地址填写下方完整地址。
3. 点击“添加到列表”，在“本次使用的镜像”中选择 GitHub。
4. 点击“保存设置”保存；需要立即检查时，点击“保存并检查更新”。

镜像目录地址：

```text
https://raw.githubusercontent.com/DaiKang777/DSH_Studio/main/updates/windows/x64/
```

请复制完整地址，保留末尾的 `/`，不要填 GitHub 仓库网页地址，也不要在后面加 `nightly.yml`。

如果当前安装版本已经是 `0.1.6-alpha.2`，提示没有新版本是正常的。发布者需要上传更高版本的安装包并更新线上说明文件，软件才能发现新版；仅上传 EXE 或改 GitHub 发布标题不会自动完成更新配置。

发现新版本后，由用户确认下载；下载完成后，再选择安装或稍后处理。不强制更新，关闭软件也不会自动安装。

下载失败时先检查网络，或在此页面切换发布者提供的其他可信镜像后重新检查。正在检查、下载或安装时，不能切换镜像。目前提供的地址只有 GitHub。
